/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you are deploying on a static-only web host, uncomment the line below:
  // output: 'export',
  // images: { unoptimized: true }, // Required if output is 'export'
};

export default nextConfig;
